# Frontend API Configuration
# Your Gemini/AI Provider API Key
VITE_API_KEY=your_api_key_here

# The external AI generation endpoint
VITE_API_ENDPOINT=https://api-ai.tegarfirman.site/v1/generate

# The local backend endpoint for saving history (relative path recommended for production)
VITE_LOCAL_API_ENDPOINT=/api

# Docker & Deployment Configuration
# The port exposed to the host machine when running via Docker Compose
APP_PORT=3002

# Cloudflare Tunnel Token (if using cloudflared service)
CLOUDFLARE_TUNNEL_TOKEN=eyJhIjoi...

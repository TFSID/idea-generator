services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: idea-generator
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3001}:3001"
    environment:
      - PORT=3001
    networks:
      - idea-generator-net

networks:
  idea-generator-net:
    driver: bridge
